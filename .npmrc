# Hoist Playwright to the root node_modules so the test runner and the spec
# files resolve a single instance of @playwright/test. Without this, pnpm's
# isolated layout loads two copies and Playwright throws "did not expect
# test.describe() to be called here".
